I"¹(function() {
  window.pickRandomColor = function() {
    var colors, el, i, j, logo, rects, ref, results, x;
    colors = ["#130051", "#3400DD", "#25009E", "#1C0077", "#2e00c4", "#220091"];
    logo = document.getElementById('logo');
    rects = logo.getElementsByTagName('rect');
    results = [];
    for (i = j = 0, ref = rects.length; j < ref; i = j += 1) {
      x = Math.floor(Math.random() * colors.length);
      el = rects[i];
      if (rects[i].id !== "background") {
        results.push(el.style.fill = colors[x]);
      } else {
        results.push(void 0);
      }
    }
    return results;
  };

  setInterval(function() {
    return pickRandomColor();
  }, 444);

}).call(this);
:ET